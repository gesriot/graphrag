[package]
name = "inih_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
# Pure std: inih is a byte-oriented line parser driving a handler callback.

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
