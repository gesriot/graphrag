[package]
name = "cjson_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
# Pure std: structure-preserving port of cJSON's node tree (Box child/next).

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
